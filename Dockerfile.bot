FROM python:3.11-slim

WORKDIR /app

# Copy dependency definition
COPY pyproject.toml ./

# Install pip and the dependencies
RUN pip install --no-cache-dir .

# Copy the rest of the application
COPY . .

ENV PYTHONPATH=/app/src

CMD ["python", "bots/telegram/app.py"]
